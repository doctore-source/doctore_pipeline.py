name: my_environment  # Specify your desired environment name
channels:
  - defaults
  - conda-forge  # Useful for some libraries, e.g., LightGBM or CatBoost
dependencies:
  - python=3.9  # Ensure you specify the Python version
  - flask
  - gunicorn
  - streamlit
  - pandas
  - numpy
  - scikit-learn
  - joblib
  - matplotlib
  - torch
  - pip  # Required for pip-based installations
  - pip:
      - torch-geometric
      - xgboost
      - lightgbm
      - catboost
      - google-auth
      - google-auth-oauthlib
      - google-auth-httplib2
      - google-api-python-client
      - google-cloud-storage
      - fpdf
      - requests
      - schedule
